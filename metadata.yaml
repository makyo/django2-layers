name: django2
summary: A full-featured layer-based charm for Django deployments
maintainer: Madison Scott-Clary <Madison.Scott-Clary@canonical.com>
description: |
  <Multi-line description here>
tags:
  - applications
  - django
  - cms
subordinate: false
provides:
  website:
    interface: http
  wsgi:
    interface: wsgi
requires:
  postgresql:
    interface: pgsql
  mysql:
    interface: mysql
  elasticsearch:
    interface: elasticsearch
  redis:
    interface: redis
  logstash:
    interface: logstash-client
  mongos:
    interface: mongodb
  cache:
    interface: memcache
